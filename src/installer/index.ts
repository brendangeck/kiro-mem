/**
 * Installer — the `kiro-mem` CLI (init, start, stop, status, uninstall).
 *
 * v1: single-machine, global install. Writes the CLI agent at
 * `~/.kiro/agents/kiro-mem.json`, deploys scripts, starts the daemon.
 */
export {};

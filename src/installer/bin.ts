#!/usr/bin/env node
/**
 * `kiro-mem` CLI entry point.
 *
 * Wired into `package.json#bin`. v0.0.0 exits with a "not implemented" notice
 * so the binary is reserved without pretending to work.
 */

console.error('kiro-mem: not implemented in this build. See AGENTS.md for the roadmap.');
process.exit(1);
